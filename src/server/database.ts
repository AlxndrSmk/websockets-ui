import { User, Game, Room, Socket, Winner } from '../types/types';

export const users: User[] = [];
export const games: Game[] = [];
export const rooms: Room[] = [];
export const connections: Socket[] = [];
export const winners: Winner[] = [];
